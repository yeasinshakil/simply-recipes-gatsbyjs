import React from 'react';
import Layout from '../components/Layout';

const tags = () => {
    return (
        <Layout>
            <div>
                <h1>hello from tags page</h1>
            </div>
        </Layout>
    );
};

export default tags;